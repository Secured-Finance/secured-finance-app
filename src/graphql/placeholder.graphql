# Placeholder query for ISSUE-058 completion
# This ensures codegen works without errors until real queries are added in ISSUE-059/060
# TODO: Remove this file in ISSUE-060 when real queries are added

query Placeholder {
  _meta {
    deployment
    hasIndexingErrors
    block {
      number
      timestamp
    }
  }
}