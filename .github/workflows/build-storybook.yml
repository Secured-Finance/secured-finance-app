name: Build Storybook

on:
  workflow_call

jobs:
  build-storybook:
    name: Build Storybook
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Build Storybook
        run: npm run build-storybook
